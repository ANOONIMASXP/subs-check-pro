version: 2

before:
  hooks:
    - go mod tidy
    - go get -u github.com/sinspired/subs-check-pro-webui
    - go install github.com/tc-hib/go-winres@latest
    - go generate ./...

release:
  # 自动检测是否为预发布版本 (如 v2.5.0-rc.1 会自动标记为 Pre-release)
  prerelease: auto

  # 底部说明模板
  footer: |
    ---
    ![GitHub Downloads (all assets, specific tag)](https://img.shields.io/github/downloads/sinspired/subs-check-pro/{{ .Tag }}/total?style=flat&logo=github&label=当前版本下载次数&labelColor=black&color=0ea5a0)

    - 💡 **完整变更日志**: [{{ .PreviousTag }}...{{ .Tag }}]({{ .GitURL }}/compare/{{ .PreviousTag }}...{{ .Tag }})
    - 🐳 Docker 镜像已发布 `docker pull sinspired/subs-check-pro:latest`
    - 💬 反馈方式
      - 👥 Telegram 群组：[subs-check性能版](https://t.me/subs_check_pro)
      - 📣 Telegram 频道：[关注频道](https://t.me/sinspired_ai)

# 变更日志配置
changelog:
  sort: desc
  use: github
  # 过滤掉不需要显示在日志中的提交
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^style:"
      - "^Merge pull request"
      - "^Merge branch"
      - "go mod tidy"
      - "^(?i)chore\\(doc\\):" # 过滤单纯的 README 调整

  format: "{{ .SHA }}: {{ .Message }}"
  # 提交分组配置
  groups:
    - title: "✨ 新功能"
      # 匹配 feat: 或 feat(scope): 且不区分大小写
      regexp: "^(?i)feat(\\([\\w-]+\\))?:+.*$"
      order: 0
    - title: "🐛 Bug 修复"
      # 匹配 fix: 或 fix(scope): 且不区分大小写
      regexp: "^(?i)fix(\\([\\w-]+\\))?:+.*$"
      order: 1
    - title: "🚀 性能优化"
      # 匹配 perf: 或 perf(scope): 且不区分大小写
      regexp: "^(?i)perf(\\([\\w-]+\\))?:+.*$"
      order: 2
    - title: "🔧 重构维护"
      # 匹配 refactor/chore/build/ci 及其带作用域的形式
      regexp: "^(?i)(chore|refactor|build|ci)(\\([\\w-]+\\))?:+.*$"
      order: 3
    - title: "📝 文档更新"
      regexp: "^(?i)docs(\\([\\w-]+\\))?:+.*$"
      order: 4
    - title: "📦 其他"
      order: 999

# 构建配置
builds:
  # 1. Linux 构建组 (使用 musl 进行静态链接)
  - id: subs-check-pro-linux
    binary: subs-check-pro
    env:
      - CGO_ENABLED=1
      # 根据架构动态切换 Zig target
      - CC=zig cc -target {{ if eq .Arch "amd64" }}x86_64-linux-musl{{ else if eq .Arch "arm64" }}aarch64-linux-musl{{ else if eq .Arch "arm" }}arm-linux-musleabihf{{ end }}
      - CXX=zig c++ -target {{ if eq .Arch "amd64" }}x86_64-linux-musl{{ else if eq .Arch "arm64" }}aarch64-linux-musl{{ else if eq .Arch "arm" }}arm-linux-musleabihf{{ end }}
    targets:
      # Desktop / Server
      - linux_amd64
      - linux_arm64
      - linux_arm_7
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
      # 添加 tags 强制 Go 使用内置 DNS 解析，防止在没有 glibc 的系统上 DNS 解析失败
      - -tags=netgo,osusergo
    ldflags:
      - -s -w -extldflags "-static -Wl,-z,stack-size=8388608" -X main.Version={{ .Version }} -X main.CurrentCommit={{ .ShortCommit }}

  # 2. Windows 构建组 (使用 gnu 兼容层)
  - id: subs-check-pro-windows
    binary: subs-check-pro
    env:
      - CGO_ENABLED=1
      - CC=zig cc -target {{ if eq .Arch "amd64" }}x86_64-windows-gnu{{ else }}aarch64-windows-gnu{{ end }}
      - CXX=zig c++ -target {{ if eq .Arch "amd64" }}x86_64-windows-gnu{{ else }}aarch64-windows-gnu{{ end }}
    targets:
      - windows_amd64
      - windows_arm64
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
    ldflags:
      # 切断对 Windows C 运行库的依赖
      - -s -w -extldflags "-static" -X main.Version={{ .Version }} -X main.CurrentCommit={{ .ShortCommit }}

  # 3. macOS 构建组 (苹果禁止静态链接，所以移除 -static 参数)
  - id: subs-check-pro-darwin
    binary: subs-check-pro
    env:
      - CGO_ENABLED=1
      - CC=zig cc -target {{ if eq .Arch "amd64" }}x86_64-macos{{ else }}aarch64-macos{{ end }}
      - CXX=zig c++ -target {{ if eq .Arch "amd64" }}x86_64-macos{{ else }}aarch64-macos{{ end }}
    targets:
      - darwin_amd64
      - darwin_arm64
    mod_timestamp: "{{ .CommitTimestamp }}"
    flags:
      - -trimpath
    ldflags:
      # macOS 绝对不能加 -extldflags "-static"
      - -s -w -X main.Version={{ .Version }} -X main.CurrentCommit={{ .ShortCommit }}

# 归档配置
archives:
  - ids:
      - subs-check-pro-linux
      - subs-check-pro-windows
      - subs-check-pro-darwin
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else if eq .Arch "arm64" }}aarch64
      {{- else if eq .Arch "arm" }}armv7
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats: zip

# Docker 镜像多架构自动打包与分发
dockers_v2:
  - id: subs-check-pro
    # 明确告诉 Docker 构建只取用 Linux 系列的产物
    ids: [subs-check-pro-linux]
    # 使用专用 Dockerfile 文件
    dockerfile: Dockerfile.goreleaser
    # 同时发布到 GitHub Container Registry 和 Docker Hub
    images:
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY }}"
      - "{{ .Env.DOCKERHUB_USERNAME }}/subs-check-pro"

    # 自动发布 latest 和 版本号
    tags:
      - "{{ .Version }}"
      - "{{ if not .IsNightly }}latest{{ end }}"

    # docker buildx 的平台列表
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/arm/v7

    # OCI 镜像规范标签
    annotations:
      "org.opencontainers.image.title": "subs-check-pro"
      "org.opencontainers.image.description": "高性能代理检测筛选工具，支持高并发测活、测速、媒体检测"
      "org.opencontainers.image.created": "{{.Date}}"
      "org.opencontainers.image.name": "{{.ProjectName}}"
      "org.opencontainers.image.revision": "{{ .FullCommit }}"
      "org.opencontainers.image.version": "{{ .Version }}"
      "org.opencontainers.image.source": "{{.GitURL}}"
      "org.opencontainers.image.base.name": "{{.BaseImage}}"
      "org.opencontainers.image.base.digest": "{{.BaseImageDigest}}"
    # 正式版本创建物料清单 (SBOM)，预发布版本跳过
    sbom: "{{ not .IsNightly }}"

# 压缩可执行文件
upx:
  - ids: [subs-check-pro-linux]
    enabled: true
    goos: [linux]
    goarch: [amd64, arm64, arm]

  - ids: [subs-check-pro-windows]
    enabled: true
    goos: [windows]
    goarch: [amd64]
